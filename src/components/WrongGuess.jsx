import React, {Components} from 'react';
import PropTypes from 'prop-types';

function WrongGuess(props){
  return (
    <div>
      <h1>Wrong pick, Try again!</h1>
    </div>
  )
};

export default WrongGuess;
